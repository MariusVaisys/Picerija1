@model ShoppingCartViewModel


@if (Model.ShopingCart.shoppingCartItems.Count > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.ShopingCart.shoppingCartItems.Count
            </span>
        </a>
    </li>
}